# Node / pnpm
node_modules/
dist/
*.tsbuildinfo

# Env (keep .env.example)
.env
!.env.example

# Prisma generated client
packages/db/generated/

# JWT RS256 dev keypair — NEVER commit the private key (public key is safe to keep)
keys/jwt-rs256-private.pem

# Objection evidence local blob-storage stub (Azure Blob is the prod target)
blob-store/
apps/objection/blob-store/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
